# Example environment variables for docker-compose
# Copy this file to .env and fill in your values

# GitHub configuration
GIT_ORG=myorg
GIT_REPOS=repo1,repo2,repo3

# Git authentication (choose one)
# Option 1: Use SSH key (default)
SSH_KEY_PATH=$HOME/.ssh

# Option 2: Use GitHub token (for HTTPS)
# GIT_TOKEN=ghp_your_token_here
# GIT_URL_FORMAT=https://github.com/{org}/{repo}.git

# Optional: Elasticsearch auth (if needed)
# ES_USERNAME=elastic
# ES_PASSWORD=changeme
